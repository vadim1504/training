package by.it.academy.java.core.race.car;

public class RacingCar extends Car {

    public RacingCar(int number, int startPosition, int finishPosition, int speed) {
        super(number, startPosition, finishPosition, speed);
    }
}
